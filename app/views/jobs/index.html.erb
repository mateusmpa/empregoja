<h1>Emprego Já</h1>
<section id="jobs">
<% @jobs.each do |job| %>
  <div>
    <h1><%= job.title %></h1>
    <p>
      <%= job.location %><br>
      <%= job.company.name %><br>
      <%= link_to 'Ver mais', job %>
    </p>
  </div>
<% end %>
</section>
<section id="companies">
<h1>Empresas</h1>
<% @companies.each do |company| %>
  <div>
    <p>
      <%= link_to company.name, company %>
    </p>
  </div>
<% end %>
</section>
